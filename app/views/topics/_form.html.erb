<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "When discussing..." %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description, "Waynebot should say..." %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit "Suggest it!(tron)" %>
  </div>
<% end %>
