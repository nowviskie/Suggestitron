<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Our good friend..." %><br />
    &nbsp;<%= f.text_field :title %>&nbsp;
  </div>
  <div class="field">
    <%= f.label :description, "should sing..." %><br />
    &#8220;<%= f.text_field :description %>.&#8221;
  </div>
  <div class="field">
    <%= f.label :url, "Video Link" %><br/>
    <%= f.text_field :url %>
  <div class="actions">
    <%= f.submit "(Freebird!)" %>
  </div>
<% end %>
